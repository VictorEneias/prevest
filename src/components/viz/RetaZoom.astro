---
/**
 * Ponte do MDX pro React. Mesma história do Juncao.astro: a diretiva client:*
 * precisa morar num arquivo que o Astro analisa estaticamente.
 */
import RetaZoomReact from './RetaZoom.tsx';
import type { RetaZoomProps } from './RetaZoom.tsx';

const props = Astro.props as RetaZoomProps;
---
<RetaZoomReact client:visible {...props} />
